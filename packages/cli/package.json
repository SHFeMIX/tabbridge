{
  "name": "@tabbridge/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "tabbridge": "dist/main.js"
  },
  "scripts": {
    "build": "tsup src/main.ts --format esm --dts --clean && node -e 'const fs = require(\"node:fs\"); const path = \"dist/main.js\"; let text = fs.readFileSync(path, \"utf8\"); if (!text.startsWith(\"#!\")) fs.writeFileSync(path, \"#!/usr/bin/env node\\\\n\" + text); fs.chmodSync(path, 0o755);'",
    "test": "vitest --run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tabbridge/broker": "workspace:*",
    "@tabbridge/shared": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/ws": "^8.5.13",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
